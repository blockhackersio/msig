{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "pub": "changeset && changeset version && changeset publish"
  },
  "devDependencies": {
    "config": "workspace:*",
    "prettier": "^3.0.3",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.9.0",
  "name": "minsig",
  "dependencies": {
    "@changesets/cli": "^2.26.2",
    "tsup": "^8.0.0"
  }
}
